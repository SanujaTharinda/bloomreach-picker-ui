# ResourceSpace Configuration - Local Development
# Copy to .env for local development with MariaDB container
#
# Usage: docker compose --profile local up --build -d

# =============================================================================
# Database (Local MariaDB container)
# =============================================================================
DB_HOST=mariadb
DB_NAME=resourcespace
DB_USER=resourcespace_rw
DB_PASSWORD=localdevpassword
MYSQL_ROOT_PASSWORD=localrootpassword

# =============================================================================
# ResourceSpace Settings - OPTIONAL
# =============================================================================
RS_PORT=8080
RS_BASE_URL=http://localhost:8080
RS_APP_NAME=Brompton DAM (Local)

# =============================================================================
# Email - OPTIONAL (not needed for local dev)
# =============================================================================
# RS_EMAIL_FROM=noreply@localhost
# RS_EMAIL_NOTIFY=admin@localhost

# =============================================================================
# Security Keys - OPTIONAL (auto-generated by RS during setup)
# =============================================================================
# Leave commented - RS will generate these during initial setup
# RS_SCRAMBLE_KEY=
# RS_API_SCRAMBLE_KEY=
