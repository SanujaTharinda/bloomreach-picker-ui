# ResourceSpace Configuration - Staging/Production
# Copy to .env and fill in values with your credentials
#
# Usage: docker compose up --build -d

# =============================================================================
# Database (Azure MySQL) - REQUIRED
# =============================================================================
DB_HOST=your-server.mysql.database.azure.com
DB_NAME=resourcespace
DB_USER=user@your-server
DB_PASSWORD=your-secure-password

# =============================================================================
# ResourceSpace Settings - OPTIONAL
# =============================================================================
# These can also be configured during the RS setup wizard
RS_PORT=80
RS_BASE_URL=https://your-resourcespace-domain.com
RS_APP_NAME=Brompton Digital Asset Management

# =============================================================================
# Email - OPTIONAL
# =============================================================================
RS_EMAIL_FROM=noreply@yourdomain.com
RS_EMAIL_NOTIFY=admin@yourdomain.com

# =============================================================================
# Security Keys - OPTIONAL (auto-generated by RS during setup)
# =============================================================================
# Only set these if migrating an existing installation
# RS_SCRAMBLE_KEY=your-existing-key
# RS_API_SCRAMBLE_KEY=your-existing-key
